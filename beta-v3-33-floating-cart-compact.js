(() => { if (window.__v333CompactCart) return; window.__v333CompactCart = true; const mobileMedia = window.matchMedia("(max-width: 767px)"); const style = document.createElement("style"); style.textContent = `@media (max-width: 767px) { .floating-cart { transition: padding .25s ease, gap .25s ease; } .floating-cart.v333-compact { padding: 10px; gap: 0; } .floating-cart.v333-compact .float-copy { max-width: 0; opacity: 0; margin: 0; overflow: hidden; transition: max-width .25s ease, opacity .15s ease, margin .25s ease; pointer-events: none; } .floating-cart:not(.v333-compact) .float-copy { max-width: 220px; opacity: 1; transition: max-width .25s ease .05s, opacity .2s ease .1s; } .floating-cart.v333-compact .float-total { margin-left: 2px; } .floating-cart .float-copy small, .floating-cart .float-copy strong { white-space: nowrap; } }`; document.head.appendChild(style); let expandTimer = null; let lastScrollY = window.scrollY; const cart = () => document.getElementById("floatingCart"); const setCompact = (compact) => { const el = cart(); if (!el) return; el.classList.toggle("v333-compact", compact); }; const scheduleExpand = (delay) => { clearTimeout(expandTimer); expandTimer = setTimeout(() => setCompact(false), delay); }; const onScroll = () => { if (!mobileMedia.matches) return; const y = window.scrollY; const delta = y - lastScrollY; lastScrollY = y; if (Math.abs(delta) < 2) return; setCompact(true); scheduleExpand(700); }; const onInteract = (event) => { if (!mobileMedia.matches) return; if (!event.target.closest("#floatingCart")) return; clearTimeout(expandTimer); setCompact(false); }; window.addEventListener("scroll", onScroll, { passive: true }); document.addEventListener("pointerdown", onInteract, true); mobileMedia.addEventListener?.("change", (event) => { if (!event.matches) setCompact(false); }); })();
