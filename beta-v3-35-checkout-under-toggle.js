(() => { if (window.__v335CheckoutMove) return; window.__v335CheckoutMove = true; const apply = () => { const toggle = document.querySelector(".v325-mode-switch"); const btn = document.querySelector(".v325-review-button"); if (!toggle || !btn) return; if (toggle.nextElementSibling !== btn) toggle.insertAdjacentElement("afterend", btn); }; const scheduleApply = () => requestAnimationFrame(() => requestAnimationFrame(apply)); const currentRender = window.render; if (typeof currentRender === "function" && !currentRender.__v335Wrapped) { const wrapped = function renderV335(...args) { const result = currentRender.apply(this, args); scheduleApply(); return result; }; wrapped.__v335Wrapped = true; window.render = wrapped; } apply(); scheduleApply(); })();
